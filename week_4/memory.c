#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int *x = malloc(3 * sizeof(int)); // we state that the address of x is allocated in the memory in whichever size it has. By multiplying it we are creating an array, because the spaces in memory are contiguous
    
}
