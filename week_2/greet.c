#include <cs50.h>
#include <stdio.h>

int main (int argc, string argv[])
{
    printf("hello, %s\n", argv[1]); // you state the argument and the position that it will take when running the program with an argument, so when we write in the terminal ./greet Maria, it uses that argument as the string to put in the placeholder. We use the position 1 because by default 0 would be the "./greet" part.
    
}
